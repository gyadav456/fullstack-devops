apiVersion: v1
kind: Secret
metadata:
  name: db-secret
  namespace: task-manager
type: Opaque
stringData:
  # In production, use SealedSecrets or ExternalSecrets
  username: "taskuser"
  password: "taskpassword"
